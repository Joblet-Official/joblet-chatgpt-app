You are acting as a senior Node.js, Express, Model Context Protocol, and OpenAI Apps SDK engineer.Work directly inside the existing Joblet ChatGPT App repository. Do not merely explain the problem. Inspect the repository, reproduce the failure, implement the necessary fixes, test them, and provide an evidence-based completion report.PROJECT GOALMake the existing Joblet job-card widget work through the modern MCP Streamable HTTP endpoint:https://joblet-chatgpt-app.onrender.com/mcpThe current legacy endpoint is:https://joblet-chatgpt-app.onrender.com/sseThe final production endpoint will eventually be:https://mcp.joblet.ai/mcpDo not remove the existing /sse implementation until /mcp has passed all acceptance tests.CURRENT SYSTEMThe application already has:1. A Node.js and Express server deployed on Render.2. The official @modelcontextprotocol/sdk.3. A functioning legacy MCP-over-SSE connection.4. A working Joblet API integration:   https://joblet.ai/api/search5. A Joblet job-card widget registered through a resource similar to:   ui://joblet/job-cards6. Job cards that render successfully when ChatGPT connects through /sse.7. Search support for job title, location, salary and remote preference.8. A minimized payload of approximately 10–12 jobs.9. A partially implemented /mcp endpoint where tools may work but the widget is reportedly not rendering.IMPORTANT TECHNICAL CORRECTIONDo not assume that ChatGPT requires the legacy /sse endpoint for widgets.The following claims from the previous diagnosis must be treated as incorrect unless proven with actual protocol traces:- “The /mcp endpoint is only a stateless HTTP POST endpoint.”- “ChatGPT requires widget HTML to be streamed slowly over a live SSE connection.”- “ChatGPT cannot render a widget from an MCP Streamable HTTP response.”- “The UI will never work through /mcp.”- “We are forced to use /sse for ChatGPT widgets.”The accurate model is:1. MCP is the protocol.2. Legacy HTTP+SSE and Streamable HTTP are transports.3. Streamable HTTP uses a single /mcp endpoint.4. Streamable HTTP supports HTTP POST and GET.5. Streamable HTTP responses may be application/json or text/event-stream.6. Therefore, /mcp can still use SSE internally when streaming is appropriate.7. The widget HTML is registered as an MCP resource.8. The tool descriptor points to that resource using _meta.ui.resourceUri.9. ChatGPT reads the resource and loads it in a sandboxed iframe.10. Tool inputs and results are delivered to the widget through the MCP Apps UI bridge.11. OpenAI’s current ChatGPT integration instructions ask for a public HTTPS /mcp endpoint.Do not describe this as choosing between “MCP and SSE.” The actual migration is from legacy HTTP+SSE transport to MCP Streamable HTTP transport.PRIMARY TASKDetermine why the widget renders through /sse but does not render through /mcp, and fix the underlying implementation.PHASE 1 — INSPECT THE REPOSITORYBefore editing anything:1. Read package.json and the lockfile.2. Record the installed versions of:   - @modelcontextprotocol/sdk   - @modelcontextprotocol/ext-apps   - express   - zod   - any Apps SDK or widget packages3. Do not upgrade blindly.4. Stay on the production-compatible v1 MCP SDK line currently used by the project unless a migration is demonstrably necessary.5. Do not move to an experimental or pre-release v2 SDK.6. Identify:   - Server entry point   - Express middleware order   - /sse handler   - /messages handler   - /mcp handler   - MCP server factory   - Tool-registration code   - Resource-registration code   - Widget HTML, JavaScript and CSS   - Render build and start commands7. Run the current build and tests.8. Reproduce the current behaviour locally before changing code.9. Create a short architecture map showing:   ChatGPT → transport → MCP server → Joblet API → tool response → widget resource.PHASE 2 — INVESTIGATE THE /MCP FAILURETest the following potential causes. Do not assume an OpenAI platform bug before eliminating implementation problems.A. Transport problemsCheck whether:1. /mcp uses the official StreamableHTTPServerTransport.2. POST /mcp correctly processes initialize, tools/list, tools/call and resources/read.3. GET /mcp is handled according to the installed SDK’s official Streamable HTTP implementation.4. DELETE /mcp is handled if stateful sessions require it.5. Content-Type and Accept headers are correct.6. application/json and text/event-stream responses are handled correctly.7. Mcp-Session-Id headers are stored and reused correctly when using stateful sessions.8. A disconnected transport is being reused.9. One McpServer instance is incorrectly being connected to multiple transports.10. A new stateless transport is being created without a corresponding initialized server.11. Express body parsing is consuming or modifying the MCP request.12. Middleware is writing to the response before the MCP transport.13. A Render proxy or compression middleware is corrupting streamed responses.14. Origin or Host validation is incorrectly rejecting ChatGPT requests.Use a shared server-construction pattern such as:- createJobletMcpServer()- registerJobletTools(server)- registerJobletResources(server)The /sse and /mcp transports may require separate McpServer instances, but they must use the same registration functions so their tools and resources cannot diverge.B. Tool metadata problemsVerify that the search tool descriptor includes:- Machine-readable name- Human-readable title- Precise description- Valid input schema- Valid output schema- Accurate annotations- _meta.ui.resourceUri pointing to the job-card resourceDo not rely only on the legacy field:openai/outputTemplateUse the current Apps SDK metadata:_meta: {  ui: {    resourceUri: "ui://joblet/job-cards-v1.html"  }}A compatibility alias may be retained only if necessary.C. Resource problemsVerify that:1. The resource URI in the tool descriptor exactly matches the registered resource.2. The resource can be retrieved through resources/read on /mcp.3. The resource appears through the same MCP server used by the /mcp tool.4. The resource returns:   text/html;profile=mcp-app5. The URI is versioned, for example:   ui://joblet/job-cards-v1.html6. The resource contains valid widget HTML.7. JavaScript and CSS assets load successfully.8. No legacy-only metadata prevents rendering through Streamable HTTP.9. The widget does not depend on data injected only during the /sse connection.10. Tool results are passed to the widget through the MCP Apps UI bridge.D. Widget problemsVerify that the widget listens for the current MCP Apps UI tool-result notification.Use the current bridge pattern, including messages similar to:ui/notifications/tool-resultThe widget should render from:structuredContentDo not assume that the raw widget HTML must be included in or streamed with every tool response.The widget resource should contain accurate metadata, including:_meta: {  ui: {    domain: "https://joblet.ai",    prefersBorder: true,    csp: {      connectDomains: [],      resourceDomains: [        "https://joblet.ai",        "https://mcp.joblet.ai",        "https://joblet-chatgpt-app.onrender.com"      ],      frameDomains: []    }  },  "openai/widgetDescription":    "Displays matching Joblet jobs in an interactive job-card carousel."}Reduce these domains to the exact minimum required by the real widget.If the widget does not directly call Joblet’s API, do not add the Joblet API to connectDomains. Prefer server-side API requests.E. Payload problemsConfirm that:1. structuredContent contains only what the model and widget need.2. Raw HTML job descriptions are removed.3. The response contains no circular objects.4. The response contains no undefined, BigInt or non-serializable values.5. Job count is capped at 10–12.6. All URLs are absolute HTTPS URLs.7. Large internal API fields are removed.8. _meta is not excessively large.9. The output exactly matches the declared output schema.PHASE 3 — DEFINE THE JOBLET TOOLS PROPERLYImplement or correct these tools.TOOL 1: search_jobsPurpose:“Search live Joblet listings and display interactive job cards. Use for initial job searches and follow-up refinements, including changes to title, location, salary, remote preference, employment type or pagination.”Suggested inputs:- query- location- remote- minimumSalary- employmentType- cursor- limitDefault limit: 10Maximum limit: 12Return:- appliedFilters- totalResults- nextCursor- jobsEach job should contain only:- id- title- company- location- salary- employmentType- summary- applyUrl- logoUrl, only if permitted and validInclude:annotations: {  readOnlyHint: true,  openWorldHint: false,  destructiveHint: false}Link the tool to:ui://joblet/job-cards-v1.htmlTOOL 2: get_job_detailsInput:- jobIdReturn:- title- company- location- salary- employmentType- sanitizedDescription- requirements- updatedDate- applyUrlThis is also read-only.OPTIONAL TOOL 3: load_more_jobsIf the widget calls this tool directly, make it app-visible but not model-visible using the current UI visibility metadata.PHASE 4 — IMPROVE FOLLOW-UP ROUTINGDo not create a Custom GPT and do not attempt to solve this with “main Custom GPT instructions.”This is an MCP-backed ChatGPT app.Set concise MCP server instructions similar to:“Use search_jobs whenever the user asks to find, browse, filter, refine,compare or show employment opportunities. For follow-up requests thatchange title, location, remote status, salary, job type or page, callsearch_jobs again using the previous filters plus the requested change.Never invent listings. If no matching jobs exist, return an empty result.”Ensure search_jobs returns appliedFilters so the model can preserve context during follow-ups.Test:1. “Find mechanical engineer jobs in Tucson.”2. “Only show remote jobs.”3. “Now show jobs paying over $120,000.”4. “Show more.”5. “Change the location to Dallas.”Every refinement should produce a new search_jobs call while the Joblet app is active.PHASE 5 — ADD PRODUCTION ROUTESThe application must expose:GET /healthGET and POST /mcpGET /ssePOST /messagesKeep /sse functioning during migration.Also add:GET /.well-known/openai-apps-challengeThis route should return only the value of:OPENAI_APPS_CHALLENGE_TOKENRequirements:- HTTP 200- Content-Type: text/plain- No JSON wrapper- No HTML- No quotation marks- No extra textIf the environment variable is not configured, return a controlled 404 or 503 without exposing server details.PHASE 6 — SECURITY AND RELIABILITYImplement:1. Server-side input validation.2. Joblet API timeouts.3. Controlled retries.4. Rate limiting.5. No API keys in the widget.6. No secrets in logs.7. No unnecessary prompt or user-data logging.8. Sanitized error responses.9. Origin and Host validation using official SDK guidance.10. Health monitoring.11. Graceful handling of no results.12. Graceful handling of expired jobs.13. Safe external apply links.14. UTM tracking without personal user identifiers.Use:utm_source=chatgptutm_medium=pluginutm_campaign=joblet_ai_job_searchPHASE 7 — REQUIRED TESTINGUse MCP Inspector:npx @modelcontextprotocol/inspector@latestTest locally:http://localhost:<port>/mcpThen test production:https://joblet-chatgpt-app.onrender.com/mcpIn Inspector, demonstrate that all of the following succeed:1. initialize2. tools/list3. resources/list4. tools/call for search_jobs5. resources/read for ui://joblet/job-cards-v1.html6. Widget render7. A second search_jobs call with refined filters8. get_job_details9. Empty results10. Invalid input handlingThen test in ChatGPT Developer Mode using the /mcp URL.Do not claim completion merely because tools/list works. Completion requires the actual Joblet widget to render from a tool call through /mcp.PHASE 8 — DIAGNOSTIC EVIDENCEIf the widget still does not render, provide:1. Exact MCP Inspector error.2. Exact HTTP status.3. Response Content-Type.4. Relevant sanitized request headers.5. Relevant sanitized response headers.6. initialize response.7. tools/list output.8. resources/list output.9. resources/read output.10. tools/call output.11. Browser console error.12. CSP error, if any.13. Comparison between the working /sse response and failing /mcp response.14. The precise line of code causing the difference.Do not label this an “OpenAI bug” unless the same valid MCP server and widget pass MCP Inspector, conform to current Apps SDK specifications, and then fail only inside ChatGPT with reproducible evidence.PHASE 9 — DELIVERABLESWhen finished, provide:1. Root-cause explanation.2. List of files changed.3. Important code changes.4. Dependency changes, if any.5. Commands used to build and test.6. MCP Inspector results.7. ChatGPT test results.8. Remaining limitations.9. Render deployment instructions.10. Environment-variable list.11. Rollback procedure.12. A final PASS/FAIL table against the acceptance criteria below.ACCEPTANCE CRITERIAThe work is complete only when:[ ] Existing /sse functionality has not been broken.[ ] /mcp supports the current MCP Streamable HTTP flow.[ ] /mcp initializes successfully.[ ] ChatGPT or Inspector can list tools through /mcp.[ ] ChatGPT or Inspector can list/read the widget resource through /mcp.[ ] search_jobs returns schema-valid structuredContent.[ ] The Joblet widget renders through /mcp.[ ] Follow-up filters trigger search_jobs again.[ ] No raw HTML descriptions or secrets appear in tool results.[ ] CSP contains only required domains.[ ] Tool annotations match actual read-only behaviour.[ ] /health works.[ ] Domain challenge route is implemented.[ ] Production build succeeds.[ ] A rollback path exists.CONSTRAINTS- Do not delete /sse yet.- Do not rebuild the entire app unnecessarily.- Do not change the Joblet public API contract without approval.- Do not add user authentication for this initial version.- Do not expose Joblet credentials to the widget.- Do not use a Custom GPT.- Do not use unsupported or experimental MCP SDK versions.- Do not provide speculative explanations without protocol evidence.- Do not deploy to production until local Inspector tests pass.- Preserve the current working job-card design unless a change is needed for Apps SDK compliance.AUTHORITATIVE DOCUMENTATIONUse these as the source of truth:OpenAI: Build your MCP serverhttps://developers.openai.com/apps-sdk/build/mcp-serverOpenAI: Connect from ChatGPThttps://developers.openai.com/apps-sdk/deploy/connect-chatgptOpenAI: Test your integrationhttps://developers.openai.com/apps-sdk/deploy/testingOpenAI: Prepare for submissionhttps://developers.openai.com/apps-sdk/deploy/submissionMCP Streamable HTTP specificationhttps://modelcontextprotocol.io/specification/2025-06-18/basic/transportsOfficial OpenAI Apps SDK exampleshttps://github.com/openai/openai-apps-sdk-examplesStart by inspecting the repository and reporting the current /sse and /mcp implementations. Then reproduce the failure before making changes. After that, implement and test the smallest correct fix.

